# Core dependencies
requests>=2.31.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Databricks SDK (for local development and testing)
databricks-sdk>=0.20.0

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.1.0
mypy>=1.7.0
types-requests>=2.31.0
pre-commit>=3.6.0

# Note: pyspark and delta-spark are pre-installed in Databricks
# Only install locally if needed for testing:
# pyspark>=3.5.0
# delta-spark>=3.0.0
